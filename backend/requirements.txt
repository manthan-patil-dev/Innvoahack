fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx>=0.27.0
python-dotenv==1.0.1
python-multipart==0.0.20

# --- Optional providers ------------------------------------------------------
# OpenAI, Groq and Gemini all speak the same /chat/completions contract and are
# called over plain httpx, so they need no SDK.
#
# Anthropic is the exception: it has its own protocol and needs its package. It
# is NOT installed by default, because it is not on the critical path — the
# Claude backend is only constructed when ANTHROPIC_API_KEY is set, and the
# import lives inside that constructor. With no key, nothing here is reachable.
#
# Only if you want the Claude backend:
#   pip install "anthropic>=0.42.0"
